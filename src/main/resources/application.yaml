spring:
  datasource:
    url: jdbc:mysql://database-1.c9giysgm0vor.ap-northeast-2.rds.amazonaws.com:3306/FitPat?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: rbflchlrh12
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발용: 스키마 자동 생성/업데이트 (운영에선 validate 권장)
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect  

  security:
    user:
      name: admin
      password: 1234
